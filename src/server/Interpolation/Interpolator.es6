export default class Interpolator {

    constructor(featuresCache, allFeatures, maxX, maxY) {
        this.featuresCache = featuresCache;
        this.allFeatures = allFeatures;
        this.maxX = maxX;
        this.maxY = maxY;
    }

    interpolate(featuresCache, allFeatures, maxX, maxY) {}
}